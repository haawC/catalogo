<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= typeof titulo !== 'undefined' ? titulo : 'Catalogo de materiales' %></title>
    <link rel="stylesheet" href="/css/estilos.css" />
  </head>
  <body>
    <header class="barra">
      <a class="marca" href="/">Catalogo de materiales</a>
      <nav>
        <% if (admin) { %>
          <a href="/admin">Panel</a>
          <form method="post" action="/admin/logout" class="inline">
            <button type="submit" class="enlace">Salir</button>
          </form>
        <% } else { %>
          <a href="/admin/login">Administracion</a>
        <% } %>
      </nav>
    </header>
    <main class="contenido">
